services:
  app1:
    build: ./app1
    ports:
      - "8081:80"
  app2:
    build: ./app2
    ports:
      - "8082:80"
  redis-db:
    image: redis:alpine
    container_name: redis-db
    volumes:
      - redis_data:/data
volumes:
  redis_data: